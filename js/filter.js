/**
 * filter.js
 * Achievement filter tabs — updated for new class names.
 * Filter buttons: .ach-filter[data-filter]
 * Achievement cards: .ach-card[data-category]
 * Hidden class: .hidden
 * Active class: .active
 */

window.initFilter = function () {
  'use strict';
  // NOTE: main.js now handles filter inline with new class names.
  // This file is kept as a no-op fallback to avoid errors.
};
